# Runtime + dev dependencies. `make setup` installs the package itself (editable)
# plus these.
#
# The Claude backend shells out to the `claude` CLI (no API key, no SDK), so
# there are no LLM-provider packages here.
sqlparse>=0.5
rich>=13.0
matplotlib>=3.7
pytest>=7.4

# A local Ollama backend is planned (see README roadmap) and needs no extra deps.
